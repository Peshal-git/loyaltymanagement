{{!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Additional Details</title>
    <link rel="stylesheet" href="/css/add-info.css">
</head>
<body>
    <h1>Additional details</h1>
    <div class="container">
    <form action="/update-dob-mobile?id={{userToUpdate.id}}" method="POST">
        <label for="mobileInput">Enter your mobile number:</label>
        <input type="tel" name="mobile" pattern="[0-9]*" value="{{#if enteredFields.mobile}}{{enteredFields.mobile}}{{/if}}">
        <br>
        <label for="dateInput">Enter your date of birth:</label>
        <input type="date" name="dob" value="{{#if enteredFields.dob}}{{enteredFields.dob}}{{/if}}">
        <br>
        <label>
            <input type="checkbox" name="hasAcceptedPrivacyPolicy" value="true" required> I agree to the Privacy Policy
        </label>
        <label>
            <input type="checkbox" name="hasGivenMarketingConsent" value="true"> I agree to receive marketing communications
        </label>
        <br>
        <input type = "submit" value="Submit">

    </form>
    {{#if error}}
            <h3 style="color: #CD5C5C;">{{error}}</h3>
        {{/if}}
</div>

</body>
</html> --}}


<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Vikasa Header</title>
    <link rel="stylesheet" href="/css/add-info.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inknut+Antiqua:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
</head>
<body>

    <header class="header">
        <div class="logo">
            <img src="/images/vikasa-logo.png" alt="Vikasa Logo">
        </div>
        <h1 class="welcome-text">Welcome to Vikasa</h1>
    </header>

    <div class="whole-form">
        <form class="registration-form" action="/update-dob-mobile?id={{userToUpdate.id}}" method="POST">
            <div class="left-column">
                <div class="form-group">
                    <label for="mobileInput">Enter your mobile number:</label>
                    <input type="tel" id="mobileInput" name="mobile" pattern="[0-9]*" value="{{#if enteredFields.mobile}}{{enteredFields.mobile}}{{/if}}" placeholder="Please enter a valid number">
                </div>
    
                <div class="form-group checkboxes">
                    <label><input type="checkbox" name="hasAcceptedPrivacyPolicy" value="true" required {{#if enteredFields.hasAcceptedPrivacyPolicy}}checked{{/if}}> I agree to the Privacy Policy</label>
                    <label><input type="checkbox" name="hasGivenMarketingConsent" value="true" {{#if enteredFields.hasGivenMarketingConsent}}checked{{/if}}> I agree to receive marketing communications</label>
                </div>
            </div>
    
            <div class="right-column">
                <div class="form-group">
                    <label for="dateInput">Enter your date of birth:</label>
                    <input type="date" id="dateInput" name="dob" value="{{#if enteredFields.dob}}{{enteredFields.dob}}{{/if}}">
                </div>
       
                <button type="submit" class="register-button">Continue</button>
            </div>
        </form>

        {{#if error}}
            <h3 style="color: #CD5C5C;">{{error}}</h3>
        {{/if}}
    </div>

</body>
</html>
