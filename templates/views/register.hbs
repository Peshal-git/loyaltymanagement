<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Registration Form</title>
    <link rel="stylesheet" href="/css/register.css">
</head>
<body>
    <div class="container">
        <h2>Registration Form</h2>
        <form action="/consent" method="post" autocomplete="off">
            <div class="form-group">
                <label for="firstname">First Name:</label>
                <input type="text" id="firstname" name="firstname" value="{{#if enteredFields.firstname}}{{enteredFields.firstname}}{{/if}}" required>
            </div>
            <div class="form-group">
                <label for="lastname">Last Name:</label>
                <input type="text" id="lastname" name="lastname" value="{{#if enteredFields.lastname}}{{enteredFields.lastname}}{{/if}}" required>
            </div>
            <div class="form-group">
                <label for="email">Email:</label>
                <input type="email" id="email" name="email" value="{{#if enteredFields.email}}{{enteredFields.email}}{{/if}}" required>
            </div>
            <div class="form-group">
                <label for="password">Password:</label>
                <input type="password" id="password" name="password" value="{{#if enteredFields.password}}{{enteredFields.password}}{{/if}}" required>
            </div>
            <div class="form-group">
                <label for="dob">Date of Birth:</label>
                <input type="date" id="dob" name="dob" value="{{#if enteredFields.dob}}{{enteredFields.dob}}{{/if}}" required>
            </div>
            <div class="form-group">
                <label for="mobile">Mobile:</label>
                <input type="number" id="mobile" name="mobile" value="{{#if enteredFields.mobile}}{{enteredFields.mobile}}{{/if}}" required>
            </div>
            <div class="form-group">
                <label for="language">Language:</label>
                <select id="language" name="language" value="{{#if enteredFields.language}}{{enteredFields.language}}{{/if}}" required>
                    <option value="English">English</option>
                    <option value="Thai">Thai</option>
                </select>
            </div>
            <button type="submit">Register</button>
        </form>
        {{#if error}}
            <h3 style="color: red;">{{error}}</h3>
        {{/if}}
    </div>
</body>
</html>
