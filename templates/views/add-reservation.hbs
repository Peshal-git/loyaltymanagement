<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Add Reservation</title>
    <link rel="stylesheet" href="/css/add-reservation.css">
    <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inknut+Antiqua:wght@300;400;500;600;700;800;900&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:opsz,wght,FILL,GRAD@20..48,100..700,0..1,-50..200" />
</head>
<body>

    <nav class="top-nav">
    <a href="/" class="dashboard-link">
      <img src="/images/vikasa-name.png" alt="Vikasa">
    </a>
    <div class="nav-links">
        <a href="/logout" class="logout-link"><span class="material-symbols-outlined">logout</span></a>
    </div>
    </nav>

<div class="main-container">
    <div class="whole-form">
        <form action="/add-reservation?id={{id}}" method="post" class="reservation-form" autocomplete="off">
            <div class="left-column">
                <div class="form-group">
                    <label for="transactionDate">Transaction Date:</label>
                    <input type="date" id="date-input" min="" name="transactionDate" class="form-input" value="{{#if enteredFields.transactionDate}}{{enteredFields.transactionDate}}{{/if}}" required>
                </div>

                <div class="form-group">
                    <label for="transactionTime">Transaction Time:</label>
                    <input type="time" name="transactionTime" class="form-input" value="{{#if enteredFields.transactionTime}}{{enteredFields.transactionTime}}{{/if}}" required>
                </div>

                <div class="form-group">
                    <label for="outletcode">Outlet Code:</label>
                    <input type="text" name="outletcode" class="form-input" value="{{#if enteredFields.outletcode}}{{enteredFields.outletcode}}{{/if}}" required>
                </div>

                <div class="form-group">
                    <label for="shiftcode">Shift Code:</label>
                    <input type="text" name="shiftcode" class="form-input" value="{{#if enteredFields.shiftcode}}{{enteredFields.shiftcode}}{{/if}}" required>
                </div>

                <div class="form-group">
                    <label for="checkNo">Check No:</label>
                    <input type="text" name="checkNo" class="form-input" value="{{#if enteredFields.checkNo}}{{enteredFields.checkNo}}{{/if}}" required>
                </div>

                <div class="form-group">
                    <label for="reference">Reference:</label>
                    <input type="text" name="reference" class="form-input" value="{{#if enteredFields.reference}}{{enteredFields.reference}}{{/if}}" required>
                </div>

                <div class="form-group">
                    <label for="folioNo">Folio No:</label>
                    <input type="number" name="folioNo" class="form-input" value="{{#if enteredFields.folioNo}}{{enteredFields.folioNo}}{{/if}}" required>
                </div>

                <div class="form-group">
                    <label for="tax">Tax:</label>
                    <input type="number" name="tax" class="form-input" value="{{#if enteredFields.tax}}{{enteredFields.tax}}{{/if}}" required>
                </div>

                <div class="form-group">
                    <label for="additionalTax">Additional Tax:</label>
                    <input type="number" name="additionalTax" class="form-input" value="{{#if enteredFields.additionalTax}}{{enteredFields.additionalTax}}{{/if}}" required>
                </div>
            </div>

            <div class="right-column">
                <div class="form-group">
                    <label for="roomNo">Room No:</label>
                    <input type="number" name="roomNo" class="form-input" value="{{#if enteredFields.roomNo}}{{enteredFields.roomNo}}{{/if}}" required>
                </div>

                <div class="form-group">
                    <label for="guestNo">Guest No:</label>
                    <input type="text" name="guestNo" class="form-input" value="{{#if enteredFields.guestNo}}{{enteredFields.guestNo}}{{/if}}" required>
                </div>

                <div class="form-group">
                    <label for="tranCode">Transaction Code:</label>
                    <input type="number" name="tranCode" class="form-input" value="{{#if enteredFields.tranCode}}{{enteredFields.tranCode}}{{/if}}" required>
                </div>

                <div class="form-group">
                    <label for="billRemark">Bill Remark:</label>
                    <input type="text" name="billRemark" class="form-input" value="{{#if enteredFields.billRemark}}{{enteredFields.billRemark}}{{/if}}" required>
                </div>

                <div class="form-group">
                    <label for="paymentRemark">Payment Remark:</label>
                    <input type="text" name="paymentRemark" class="form-input" value="{{#if enteredFields.paymentRemark}}{{enteredFields.paymentRemark}}{{/if}}" required>
                </div>

                <div class="form-group">
                    <label for="paymentFlag">Payment Flag:</label>
                    <input type="text" name="paymentFlag" class="form-input" value="{{#if enteredFields.paymentFlag}}{{enteredFields.paymentFlag}}{{/if}}" required>
                </div>

                <div class="form-group">
                    <label for="amount">Amount:</label>
                    <input type="number" name="amount" class="form-input" value="{{#if enteredFields.amount}}{{enteredFields.amount}}{{/if}}" required>
                </div>

                <div class="form-group">
                    <label for="service">Service:</label>
                    <input type="number" name="service" class="form-input" value="{{#if enteredFields.service}}{{enteredFields.service}}{{/if}}" required>
                </div>

                <button type="submit" class="confirm-button">Confirm reservation</button>
                {{#if error}}
                    <h3 style="color: #CD5C5C;">{{error}}</h3>
                {{/if}}
            </div>
        </form>
    </div>
</div>
</body>
<script>
    var today = new Date().toISOString().split('T')[0];
    document.getElementById("date-input").setAttribute("min",today);
</script>
</html>
